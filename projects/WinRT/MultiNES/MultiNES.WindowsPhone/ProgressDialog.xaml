<ContentDialog
    x:Class="MultiNES.ProgressDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MultiNES"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    x:Uid="ProgressDialog"
    Title="DIALOG TITLE"
    PrimaryButtonText="Primary"  
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick">

    <StackPanel VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
        <!-- Content body -->
        <TextBlock x:Name="message" Style="{StaticResource MessageDialogContentStyle}" TextWrapping="Wrap" FontSize="24" Margin="0,0,0,20">
        </TextBlock>
        <ProgressRing IsActive="True" Background="{x:Null}"></ProgressRing>
    </StackPanel>
</ContentDialog>
